declare module "ai" {
  interface UIMessageDataByType {
    // Custom UI event for showing the TelemetryPanel
    "data-telemetryShow": {
      sensorId: string;
      personaName?: string;
    };
  }
}
